extends layout

block content

    p Do you really want to delete this Book Instance?

    form(method='POST')
      div.form-group
        input#bookinstance.form-control(type='hidden', name='bookinstanceid', value=bookinstanceid )

      button.btn.btn-primary(type='submit') Delete
